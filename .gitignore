__pycache__
.pytest_cache
*.egg-info
.vscode/
.cursor/
.agent/
.kiro/
.windsurf/
.windsurfrules
.venv/
.torrents/
.music/
temp/
dist/
build/

test.py
smoked.db*
config.py
config.toml
accounts.json
lox/web/static/specs
!.gitkeep
.venv

# lox deployment
.env
config/config.toml
config/state/
data/music/
data/links/
data/torrents/
data/tmp/
# Each suite writes its own settings, state and downloads under tests/_<name>.
# Listing them one at a time meant every new suite left untracked directories
# behind until someone noticed.
tests/_*/

# Runtime proof-of-life from tools/cineplex_seat_watch.py, rewritten every
# poll -- tracking it means the tree is dirty the whole time it's watching.
tools/seat_watch_heartbeat.txt
